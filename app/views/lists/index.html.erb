<div class= 'container'>
  <h1>Lists</h1>

  <%= cl_image_tag "sample", width: 400, height: 300, crop: :fill %>
  <div class='summary-list'>
    <% @lists.each do |list| %>
      <div class='list'><%= link_to list.name, list_path(list) %></div>
    <% end %>
  </div>
</div>
